"use strict"; //treat all JS code as newer version

//alert(3+3) // since we are using nodejs, not browser so it will cause error

let name="hitesh" //string datatype
let age=18 //number (2^53) also use 'bigint'
let isLoggedIn=false//boolean
//null => is a standalone value
//undefined => if the variable is not given a value
//symbol =>used for uniqueness


//object

console.log(typeof undefined); //undefined
console.log(typeof null); // will give object